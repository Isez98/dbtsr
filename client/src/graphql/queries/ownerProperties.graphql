query OwnerProperties($id: Int!, $limit: Int!, $cursor: String) {
  ownerProperties(id: $id, limit: $limit, cursor: $cursor) {
    id
    designation
    ownerId
    owner {
      name
    }
    developmentId
    development {
      name
    }
    album
    notes
  }
}
